# DocuMind
A retrieval-augmented generation (RAG) system that allows users to query PDF documents in natural language. The application uses FAISS embeddings and OpenAI GPT models to provide context-aware answers through a Flask backend and Streamlit interface.
